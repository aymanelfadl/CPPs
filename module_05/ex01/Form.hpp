#pragma once 

#include <iostream>

class Form {
    private:
        
        const std::string formName;
        bool isSigned;
        const int gradeToSign;
        const int gradeToExecute;

    public:

        Form();
        Form();
        Form();

}